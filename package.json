{
  "name": "pharos",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "frontend:build": "npm --prefix apps/frontend run build",
    "packages:build": "npm run build --workspace=@packages/types && npm run build --workspace=@packages/utils && npm run build --workspace=@packages/enum"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
