{
  "name": "pharos",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=@packages/types && npm run build --workspace=@packages/utils && npm run build --workspace=@packages/enum"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
